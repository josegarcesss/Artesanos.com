html
    head
        title Artesanos.com - Feed Principal
        meta(charset="UTF-8")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet" href="/css/style.css")
    body(data-user-id=user.id_usuario)
include navbar

block content
    .container
        h2 Galería de Obras Públicas

        if images && images.length > 0
            .gallery
                each image in images
                    .image-card
                        img(src=image.url_imagen alt=image.titulo)
                        .image-info
                            .image-title= image.titulo || 'Sin título'
                            .image-author Autor: #{image.autor}
                            a(href="/image/" + image.id_imagen) Ver detalles →
        else
            p No hay imágenes públicas aún.

        a(href="/") Volver al inicio
script(src="/socket.io/socket.io.js")
script(src="/js/notifications.js")